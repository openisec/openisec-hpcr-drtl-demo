feat: sync dev content to demo ? Terraform (Model Armor, IAM cleanup, cloudsql wiring with prd-grade REGIONAL/PITR/deletion protection preserved), ci-cd.yml (action versions, API test fixes, dynamic FRONTEND_URL via deployed web URL preserved), API (audit logging, mail service, model armor, rate limiting, newer Gemini SDK, 17 migrations, CORS env-based), Web (new pages, nonce CSP via proxy.ts, next 16.3.3)
